# requirements.txt — FINAL VERSION (Nov 2025)
# Copy-paste this exactly into your Streamlit app

streamlit==1.40.0
streamlit-authenticator==0.3.2
pandas==2.2.3
plotly==5.24.1
pyyaml==6.0.2
openpyxl==3.1.5

# Optional but recommended for better performance & security
gunicorn==23.0.0
watchdog==5.0.3

# Note: No need for smtplib, email, datetime — they are built-in Python
